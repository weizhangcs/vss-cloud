# 文件路径: .env.template
# 描述: VSS-Cloud 环境变量模板
# 用法: 复制为 .env 并填入实际值 (通常由 init.sh 自动生成)

# ==============================================================================
# [第一类] 核心基础配置 (CORE INFRASTRUCTURE)
# ------------------------------------------------------------------------------
# 说明: 项目启动和运行的硬性依赖。必须在部署阶段通过文件 (init.sh) 确定。
# ==============================================================================

# --- 1.1 安全与网络 (Security & Network) ---
# [自动生成] Django 密钥
SECRET_KEY=
# [必填] 运行模式 (生产环境必须为 False)
DEBUG=False
# [必填] 允许的主机/域名，逗号分隔
ALLOWED_HOSTS=
# [必填] CSRF 防护机制信任的访问源，协议://主机/域名:端口，逗号分隔
CSRF_TRUSTED_ORIGINS=

# [必填] 服务器域名 (用于 Nginx 配置)
SERVER_DOMAIN=

# --- 1.2 数据库连接 (Database) ---
# [自动生成] 数据库连接 URL
DATABASE_URL=
POSTGRES_DB=
POSTGRES_USER=
# [自动生成] 数据库密码
POSTGRES_PASSWORD=

# --- 1.3 缓存与消息队列 (Cache & Broker) ---
# Redis 连接
REDIS_URL=redis://redis:6379/0


# ==============================================================================
# [第二类] 第三方集成账户 (THIRD-PARTY INTEGRATIONS)
# ------------------------------------------------------------------------------
# 说明: Google Cloud / Aliyun 等外部服务的凭证与端点。
# 注意: 当前版本暂存于 .env。在后续开发计划中，这些配置将迁移至 Django Admin
#       进行动态配置 (Dynamic DB Configuration)，不再依赖环境变量。
# ==============================================================================

# --- 2.1 Google Cloud Platform (Vertex AI / GCS) ---
# [必填] 项目 ID
GOOGLE_CLOUD_PROJECT=
# [必填] 区域 (如 us-central1)
GOOGLE_CLOUD_LOCATION=
# [必填] GCS 默认存储桶 (用于 RAG/文件交换)
GCS_DEFAULT_BUCKET=
# [必填] Google API Key (Gemini)
GOOGLE_API_KEY=
# (注: 凭证文件 conf/gcp-credentials.json 也是此类配置的一部分)

# --- 2.2 Aliyun PAI-EAS (CosyVoice) ---
# [选填] PAI-EAS 服务地址
PAI_EAS_SERVICE_URL=
# [选填] PAI-EAS 访问令牌
PAI_EAS_TOKEN=


# ==============================================================================
# [第三类] 应用层业务配置 (APPLICATION CONFIG & PRICING)
# ------------------------------------------------------------------------------
# 说明: 相对稳定的业务常量（如定价策略、汇率）。
# 注意: 当前版本暂存于 .env。后续计划迁移至 Django Admin 动态配置。
# ==============================================================================

# --- 3.1 计费基础 ---
USD_TO_RMB_EXCHANGE_RATE=7.25

# --- 3.2 Gemini 模型定价策略 (Vertex AI) ---
# 仅在 Google 调整官方定价时修改
# --- Gemini 2.5 Pro (200K 阈值分层) ---
# 标准层: Prompt <= 200K tokens; 长层: Prompt > 200K tokens
GEMINI_2_5_PRO_THRESHOLD_TOKENS=200000
GEMINI_2_5_PRO_INPUT_USD_STANDARD=1.25
GEMINI_2_5_PRO_OUTPUT_USD_STANDARD=10.00
GEMINI_2_5_PRO_INPUT_USD_LONG=2.50
GEMINI_2_5_PRO_OUTPUT_USD_LONG=15.00

# --- Gemini 2.5 Flash (平价) ---
# 阈值设置为极大值，确保它只走 'standard' 价格
GEMINI_2_5_FLASH_THRESHOLD_TOKENS=9999999
GEMINI_2_5_FLASH_INPUT_USD_STANDARD=0.30
GEMINI_2_5_FLASH_OUTPUT_USD_STANDARD=2.50

# --- Gemini 2.5 Flash-Lite (平价) ---
GEMINI_2_5_FLASH_LITE_THRESHOLD_TOKENS=9999999
GEMINI_2_5_FLASH_LITE_INPUT_USD_STANDARD=0.10
GEMINI_2_5_FLASH_LITE_OUTPUT_USD_STANDARD=0.10